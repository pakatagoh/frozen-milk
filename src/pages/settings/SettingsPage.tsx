// Replaced by routes/settings.tsx (layout) + routes/settings.index.tsx (menu).
// This component is no longer used. File kept temporarily for reference;
// it will be cleaned up with _old_SettingsPage.tsx.
export function SettingsPage() {
  return null;
}
